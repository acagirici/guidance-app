<h1> Welcome <%= current_user.username.titleize %> </h1>

<h1><%= display_students %></h1> 
<ul>
    <% current_user.students.each do |s|%>
      <li><%= s.first_name %> <%= s.last_name%> (<%= link_to "Student Page", student_path(s) %>) </li>
    <% end %>
</ul>